# ============================================
# SolMind Frontend Environment Variables
# ============================================
# Copy this file to .env in the project root
# cp env.example .env

# Backend API URL
# This is the URL where your FastAPI backend is running
# Default: http://localhost:8000
# The vite.config.ts has a proxy configured for /api, so this is optional
# Only set this if your backend is running on a different URL
VITE_API_BASE_URL=http://localhost:8000

# Optional: Supabase Configuration (if using client-side Supabase)
# VITE_SUPABASE_URL=your_supabase_project_url
# VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Optional: Analytics or other services
# VITE_ANALYTICS_ID=your_analytics_id

